# Step 04a — Load frozen PCA axes

## Goal

Resolve the frozen axis cache built in Step 00d.

## Scripts

`adna/pca_lock.py`, `project.py` (load path), `core/dosage.resolve_cache`

## Statistical / file outputs

Axis files + eigenvalue fractions ready for projection.

## Visualization outputs

None until 04b.
