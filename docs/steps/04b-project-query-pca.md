# Step 04b — Project query onto PCA

## Goal

Least-squares (or GCTA-consistent) projection of the query onto frozen axes.

## Scripts

`grapeancestry project` · `adna/project.py`, `run_project.py`

## Statistical / file outputs

| Artifact | Meaning |
|----------|---------|
| Query PC1/PC2/PC3 | Header + `results/{sample}.pca.tsv` when written |

## Visualization outputs

Interactive 2D/3D PCA (query star; pin yellow diamond).
