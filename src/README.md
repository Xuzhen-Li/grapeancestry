# src/

Python package **`grapeancestry`** — CLI entry `grapeancestry` after `pip install -e .`.

Domains: `core`, `identity`, `adna`, `popgen`, `breeding`, `report`, `cloud`, `resource`. Step docs: `steps/`.
